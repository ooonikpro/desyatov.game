import generateNumberList from "@shared/lib/generateNumberList";

const olderYear = 80;
const youngYear = 10;

export const defaultYear = 25;

export const yearsList = generateNumberList(youngYear, olderYear);
